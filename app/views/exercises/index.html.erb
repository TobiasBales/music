<%= render(Bootstrap::Page.new(title: "Exercises")) do %>
  <%= render(Bootstrap::List::Group.new) do %>
    <% @exercises.each do |exercise| %>
      <%= render(Bootstrap::List::Item.new(text: exercise.name, to: exercise)) %>
    <% end %>
  <% end %>
<% end %>
