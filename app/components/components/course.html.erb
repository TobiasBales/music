<%= render(Bootstrap::Card.new(
  title: course.name,
  image: course.thumbnail)) do |c|%>
  <% c.with_body do %>
    <%= render(Bootstrap::Button.new(text: button_text, to: course_path(course))) %>
  <% end %>
<% end %>
